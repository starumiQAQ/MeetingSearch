# Deliver as a local web page

The meeting search is a local web UI (form in, Ranking out), not a CLI. Sharing results and picking ambiguous geocode matches are interactive; a browser page fits better than a terminal for this product.
